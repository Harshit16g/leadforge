"use client";

/**
 * DateRangePicker — re-export alias for CalendarRange.
 * Keeps existing imports working while CalendarRange is the canonical impl.
 */

export { CalendarRange as DateRangePicker } from "@/components/ui/calendar-range";
export type { } from "@/components/ui/calendar-range";
